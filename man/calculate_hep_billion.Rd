% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_hep_billion.R
\name{calculate_hep_billion}
\alias{calculate_hep_billion}
\title{Calculate HEP Billion}
\usage{
calculate_hep_billion(
  df,
  scenario_col = NULL,
  transform_value_col = "transform_value",
  level_col = stringr::str_replace(transform_value_col, "transform_value", "level"),
  contribution_col = stringr::str_replace(transform_value_col, "transform_value",
    "contribution"),
  contribution_pct_col = paste0(contribution_col, "_percent"),
  start_year = 2018,
  end_year = 2019:2025,
  pop_year = 2025,
  ind_ids = billion_ind_codes("hep", include_calculated = TRUE)
)
}
\arguments{
\item{df}{Data frame in long format, where 1 row corresponds to a specific
country, year, and indicator.}

\item{scenario_col}{Column name of column with scenario identifiers. Useful for
calculating contributions on data in long format rather than wide format.}

\item{transform_value_col}{Column name of column(s) with transformed indicator values,
used to calculate contributions.}

\item{level_col}{Column name of column(s) with indicator levels. Should be same length
as \code{transform_value_col}.}

\item{contribution_col}{Column name of column(s) to store contribution (population)
values. Must be the same length as \code{transform_value_col}.}

\item{contribution_pct_col}{Column name of column(s) to store contribution (percent)
values. Must be the same length as \code{transform_value_col}.}

\item{start_year}{Base year for contribution calculation, defaults to 2018.}

\item{end_year}{End year(s) for contribution calculation, defaults to 2019 to 2025.}

\item{pop_year}{Year used to pull in HPOP populations, defaults to 2025.}

\item{ind_ids}{Named vector of indicator codes for input indicators to the Billion.
Although separate indicator codes can be used than the standard, they must
be supplied as a named vector where the names correspond to the output of
\code{billion_ind_codes()}.}
}
\value{
Data frame in long format.
}
\description{
\code{calculate_hep_billion()} calculates country-level HEP Billion based on
the component indicators. It calculates the change for Prevent and Prepare between
a start year and end year, and estimated the change for Detect and Respond based on
its level in the end year. If data is not available for the end year for Detect and
Respond, the latest year of observed data is used. Details are available in
the methods report for the exact method applied.
}
\details{
For more details on the HEP Billion calculation process and how this function
ties in with the rest, see the vignette:

\href{../doc/hep.html}{\code{vignette("hep", package = "billionaiRe")}}
}
